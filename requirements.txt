# tiny_corpus_prep requirements
# 
# Install with UV (recommended):
#   uv pip install -e ".[annotators]"
#
# Or with pip:
#   pip install -e ".[annotators]"

# Core dependencies
polars>=0.19.0
textstat>=0.7.0
pyarrow>=14.0.0
tqdm>=4.65.0

# Optional: Annotation support (install with [annotators] extra)
# google-generativeai>=0.3.0
# python-dotenv>=1.0.0

